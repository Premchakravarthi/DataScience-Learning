install:

pip install streamlit

run a .py: python filename.py
            python ex1.py


streamlit run: python -m streamlit run filename.py

When we run this fro the first time it might be ask enter a emailid
read the terminal output properly
after that a new page will open in a new web browser and that is our app.

we can write many python files

import streamlit as st
st.title("MY FIRST APP")
st.write("im happy now")